# =============================================================================
#  LARK – Python dependencies
#  Install with:  pip install -r requirements.txt
#               (setup.sh installs these into .venv automatically)
#
#  System dependencies (apt) → installed by setup.sh:
#    sudo apt install python3-libiio libiio-utils libad9361-dev
#    sudo apt install python3-pyqt5 rtl-sdr soapysdr-module-rtlsdr
# =============================================================================

# ── DSP core ─────────────────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.11

# ── Plotting ─────────────────────────────────────────────────────────────────
matplotlib>=3.7

# ── PyQt5 (matplotlib backend + KrakenSDR GUI) ───────────────────────────────
# Prefer: sudo apt install python3-pyqt5
# pip fallback (uncomment if not installed via apt):
# PyQt5>=5.15

# ── LibreSDR / AD9363 via pyadi-iio ──────────────────────────────────────────
# Requires libiio installed via apt (python3-libiio) or pip
pyadi-iio>=0.0.17

# ── Protobuf — gnss-sdr UDP monitoring ───────────────────────────────────────
protobuf>=4.21

# ── Test runner ──────────────────────────────────────────────────────────────
pytest>=7.0

# ── Packaging utilities ──────────────────────────────────────────────────────
packaging>=23.0
