---
description: Core LARK project rules for all agent sessions
alwaysApply: true
---

# LARK — core agent rules

Read **AGENTS.md** at repo root for paths, conventions, and commands.

## Non-negotiable

- Work from `krakenSDR/src/` for Python commands and imports.
- Reusable DSP → `core/`; CLI/viz → `apps/doa_iridium/`.
- Azimuth: 0°=North, clockwise. Elevation: above horizon.
- Do **not** edit `external/` submodules unless explicitly requested.
- Do **not** create git commits unless the user explicitly asks.
- Minimize diff scope; match existing code style.

## Pointers

- Operations: `krakenSDR/src/apps/doa_iridium/README.md`
- Architecture: `docs/architecture.md`
- Pipeline theory: `docs/doa-pipeline.md`
